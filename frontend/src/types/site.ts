export type NavItem = {
  label: string;
  href: string;
};

export type ServiceItem = {
  title: string;
  description: string;
  image: string;
  label: string;
};

export type HighlightItem = {
  code: string;
  title: string;
  description: string;
  image: string;
};

export type SectorItem = {
  name: string;
};

export type HeroSlide = {
  title: string;
  subtitle: string;
  sector: string;
  metric: string;
  surface: string;
  image: string;
};
